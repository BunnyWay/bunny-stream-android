<resources>
    <!--
        NOTE: This library must NOT declare a style named "Theme.BunnyStream".
        That name belongs to the demo app; declaring it here leaks into the app's
        resource merge (this library's values-night variant used to override the
        app's NoActionBar theme in dark mode, producing a stray black DarkActionBar
        titled "Bunny Stream" and broken edge-to-edge layout on Android 15+).
        TV activities declare their themes directly in this module's manifest.
    -->
</resources>
